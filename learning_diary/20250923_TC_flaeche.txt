🔸 🔹 ❗ ❓(memo tools)

🔸today's plan : 
	DefaultDocument.cs	add MEOTHDE(set 'cacheAreaAdjusted' as -1, sothat flaeche is recaculated)

🔸what i did : 
	2.5std arbeiten

🔸	meaning
	IDocument.cs = class like a Contract(계약서), that is usually used in C# projects. (=~ rulebook with only methode names, parameters, and return types)
	DefaultDocument(something like this) = classes, that implement(구현)

🔸	changed (tag author : dongwon jeong)
	IDocument.cs (made a new funtion)
void InvalidateDependentAreaCaches(Polyline changed);
	DefaultDocument.cs (

-problem was : 넓이가 처음 계산됨. 그 다음부터는 캐시를 이용해서 불러와짐. 그래서 다른 도형이 숨겨지든, 없어지든 상관없이 캐시가 유지됨
			--> 캐시를 초기화 하는 methode를 만듬


❗ChatGPT에게 코딩할 때마다 할 질문
C#뿐만 아니라 프로그래밍의 철학까지 배우고싶어. 그래야 내 머릿속에 구조가 선명하게 각인될 거고, 나중에 다른 프로그래밍 업무를 볼 때, 파일들이 어떤 구조를 가지고 있는지 빨리 파악할 수 있게 될테고, 흐름을 보다 쉽게 알 수 있을테니까. 지금은 코딩을 잘 못해서 theoCAD에 문제가 생겨도 어떤 파일부터 봐야 되는지 감도 안잡히고, 그냥 너가 말하는 코드들을 붙여 넣고만 있지만 나중에는 내가 직접 문제가 있을 만 한 파일들을 알아나고 싶어. 너에게 전체 솔류선을 업로드 하고, 파일을 찾아달라고 할 수는 없잖아. 너무 용량이 크거든.

여기서 질문은 : 일반적인 C#설계구조는 어떻게 되어있는지 우선 간략하게 설명해줄 수 있어? CAD프로그램뿐만 아니라 다른 것도 유사한지 궁금해. 
그리고 다음 질문은 : 나중에 너와의 대화를 통해 너가 코드를 제시하거나 설명을 할거잖아. 그 때 나에게 C#의 설계의 관한 내용이라면 살짝살짝 알려줘. 내가 전체적인 구조를 배울 수 있게


❗호출자 추적. 상위 methode찾는 방법. (대규모 솔루션을 읽는 방법) (하위로 가는 방법은 파라메터를 읽자)
drawLabels에 issue가 있다고 생각함 -> ctrl f / find all / drawLabels -> 'drawShortInfo' methode가 drawLabels을 호출중 -> ctrl f / find all / drawShortInfo -> 반복 -> DrawMouseOverInfo -> Draw(Graphics g)까지 도착.
이 과정을 반복하면 5000줄짜리 파일도 20~30줄짜리 "호출 경로" 그림으로 압축할 수 있음.


❗ next time :